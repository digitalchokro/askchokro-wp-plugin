{
  "name": "askchokro-wp-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "lint": "wp-scripts lint-js",
    "zip": "zip -r askchokro-wp-plugin.zip . -x 'node_modules/*' -x 'src/*' -x '.git/*' -x 'package-lock.json' -x 'package.json' -x 'webpack.config.js' -x '*.zip'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^32.6.0"
  },
  "dependencies": {
    "recharts": "^3.9.1"
  }
}
